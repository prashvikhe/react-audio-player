import React from 'react';
import '../styles/index.css'; // Import index.css from the styles folder

const TopBar = () => {
  return (
    <div className="top__bar">
      <h1 className="title">IoT<span className="iot">Ready</span> Assignment</h1>
    </div>
  );
};

export default TopBar;
